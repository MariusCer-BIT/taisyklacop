<!DOCTYPE html>
<html lang="en">
    {{>head}}
    <body id="page-top">
        {{>nav}}
            <section class="page-section mt-5" >
                <div class="container">
                    <h2 class="page-section-heading text-center text-uppercase text-secondary mt-2 mb-4">
                       {{ title }}
                    </h2>
                    
                </div>
                 <div class="row justify-content-center">
                    <div class="col-lg-8 col-xl-7">
                        {{#each registracija}}
                        <div class="card mb-4">
                            <div class="card-body">
                                <h5 class="card-title">{{name}}</h5>
                                 <p class="card-title">{{phone}}</p>
                                 <p class="card-title">{{carmanuf}}</p>
                                  <p class="card-title">{{carmodel}}</p>
                                   <p class="card-title">{{caryear}}</p>
                                    <p class="card-title">{{reapairdate}}</p>
                                <p class="card-text">{{text}}</p>
                            </div>
                        </div>
                        {{~/each}} 
                      
                        </form>
                    </div>
                </div>    


            </section>
        {{>footer}}
    </body>
</html>
