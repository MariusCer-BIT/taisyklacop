<!DOCTYPE html>
<html lang="en">
    {{>head}}
    <body id="page-top">
        {{>nav}}
            <section class="page-section mt-5" >
                <div class="container">
                    <h2 class="page-section-heading text-center text-uppercase text-secondary mt-2 mb-2">
                        {{ title }}
                    </h2>
                    
                </div>
                <div class="row justify-content-center">
                    <div class="col-lg-8 col-xl-7">
                         
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Pavadinimas</th>
                                    <th>Kaina</th>
                                </tr>
                            </thead>
                            <tbody>
                                {{#each paslaugos}}
                                <tr>
                                    <td>{{name}}</td>
                                    <td>{{price}} EUR</td>
                                </tr>
                                {{~/each}}
                            </tbody>
                        </table>
                    </div>
                   
                </div>


            </section>
        {{>footer}}
    </body>
</html>
